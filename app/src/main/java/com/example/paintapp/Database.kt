package com.example.paintapp

/*
TODO: Database class: Keep track of path in SQLLite + save file (bitmap) on device (RECOMMENDED)
have user enter a filename
store the file name use in database (SQLLite)
actual drawing stored on device (Dir folder -special folder for the application
*/
class Database {
}